<div class="row ">

        <div class="col-2">
            {{number}}
            <h5>{{status}}</h5>
        </div>
        <div class="col-2">
            {{#with amount}}
            <p>
                {{title}}
            </p>
            <h5>{{value}}</h5>
            {{/with}}
        </div>
        <div class="col-7 ">
            {{#each images}}
            <img src="{{this}}" alt="photos">
            {{/each}}
        </div>